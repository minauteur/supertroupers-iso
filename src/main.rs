extern crate supertroupers_iso;
extern crate router;
extern crate iron;
use router::Router;
use supertroupers_iso::{util, http, poems, search, gen};

fn main() {
    let mut router = Router::new();

    // router.get()
    println!("Hello, world!");
}
